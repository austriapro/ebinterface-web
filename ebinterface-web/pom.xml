<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>at.austriapro</groupId>
    <artifactId>ebinterface-web-parent</artifactId>
    <version>1.0.7-SNAPSHOT</version>
  </parent>
  <artifactId>ebinterface-web</artifactId>
  <packaging>war</packaging>
  <version>1.0.7-SNAPSHOT</version>
  <name>ebinterface-web</name>

  <description>The ebInterface validation web interface offers a web-based interface for the ebinterface-core validation service</description>
  <url>http://ebinterface.at</url>

  <organization>
    <url>http://ebinterface.at</url>
  </organization>

  <properties>
    <ebinterface-mapping-version>1.0.0-SNAPSHOT</ebinterface-mapping-version>
    <ebinterface-rendering-version>1.0.1-SNAPSHOT</ebinterface-rendering-version>
    <ebinterface-ubl-mapping-version>4.0.1-SNAPSHOT</ebinterface-ubl-mapping-version>
    <ebinterface-xrechnung-mapping-version>1.0.0-SNAPSHOT</ebinterface-xrechnung-mapping-version>
    <logback-version>1.2.3</logback-version>
    <wicket-version>6.30.0</wicket-version>
  </properties>

	<dependencies>
		<!-- ebInterface core for the validation logic -->
		<dependency>
			<groupId>at.austriapro</groupId>
			<artifactId>ebinterface-web-core</artifactId>
			<version>${project.version}</version>
		</dependency>

		<!-- ebInterface rendering for PDF rendering and ZUGFeRD generation-->
		<dependency>
			<groupId>at.austriapro</groupId>
			<artifactId>ebinterface-rendering</artifactId>
			<version>${ebinterface-rendering-version}</version>
		</dependency>

		<!-- ebInterface mappings for ebInterface -> ZUGFeRD mapping-->
		<dependency>
			<groupId>at.austriapro</groupId>
			<artifactId>ebinterface-mappings-zugferd</artifactId>
			<version>${ebinterface-mapping-version}</version>
		</dependency>

    <dependency>
      <groupId>com.helger</groupId>
      <artifactId>ebinterface-ubl-mapping</artifactId>
      <version>${ebinterface-ubl-mapping-version}</version>
    </dependency>
    
    <dependency>
      <groupId>com.helger</groupId>
      <artifactId>ebinterface-xrechnung-mapping</artifactId>
      <version>${ebinterface-xrechnung-mapping-version}</version>
    </dependency>

    <!-- Wicket -->
    <dependency>
      <groupId>org.apache.wicket</groupId>
      <artifactId>wicket-core</artifactId>
      <version>${wicket-version}</version>
    </dependency>
    
    <dependency>
      <groupId>com.helger</groupId>
      <artifactId>ph-oton-jetty</artifactId>
      <version>8.2.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>${basedir}/src/main/resources</directory>
        <targetPath>${project.build.directory}/classes</targetPath>
      </resource>
      <resource>
        <directory>${basedir}/src/main/webapp</directory>
        <targetPath>${project.build.directory}/webapp-classes</targetPath>
      </resource>
      <resource>
        <directory>${basedir}/src/test/resources</directory>
        <targetPath>${project.build.directory}/test-classes</targetPath>
      </resource>
    </resources>
  </build>
</project>
